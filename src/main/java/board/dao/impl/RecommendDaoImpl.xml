<?xml version="1.0" encoding="UTF-8"?>

<!-- 마이바티스 3 Mapper DOCTYPE -->
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board.dao.face.RecommendDao">
	<select id="selectCountRecommend"
	 parameterType="dto.Board"
	 resultType="int">
		select count(*) from recommend
		where member_idx=#{member_idx} and board_idx=#{board_idx}
	</select>
	
	<insert id="insertRecommend" 
	 parameterType="dto.Board">
		insert into recommend (recommend_idx, member_idx, board_idx)
		values(recommend_seq.nextval, #{member_idx}, #{board_idx})
	</insert>
	
	<delete id="deleteRecommend"
	 parameterType="dto.Board">
	 	delete from recommend
	 	where member_idx=#{member_idx} and board_idx=#{board_idx}
	</delete>
	
	<select id="selectTotalRecommend"
	 parameterType="dto.Board"
	 resultType="int">
	 	select count(*) from recommend
	 	where board_idx=#{board_idx}
	</select>
</mapper>
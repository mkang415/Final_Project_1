<?xml version="1.0" encoding="UTF-8"?>

<!-- 마이바티스 3 Mapper DOCTYPE -->
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="qna.dao.face.QnaDao">
	
	<!-- qna 개수 가져오기 -->
	<select id="qnaCnt"
	 parameterType="util.QnaPaging"
	 resultType="int">
		select count(*) from qna where frequency = 1
		<if test="search != null and search !=''">
			and title like '%' || #{search} || '%'
		</if>
	</select>
	
	<!-- qna 리스트 가져오기 -->
	<select id="getNewList"
	 parameterType="util.QnaPaging"
	 resultType="hashmap">
		select * from (
			select rownum rnum, B.* from (
				select qna_idx, title, question, answer, member.nickname, 
					question_date, answer_date, frequency 
				from qna, member
				where qna.member_idx = member.member_idx and frequency = 1
				<if test="search != null and search != ''">
	 				AND title LIKE '%' || #{search } || '%'
	 			</if>
				order by qna_idx desc
			) B order by rnum
		) qna
		where rnum between #{startNo} and #{endNo}
	</select>
	
	<select id="qnaView"
	 parameterType="int"
	 resultType="dto.Qna">
		select qna_idx, title, question, answer, member.nickname, 
			question_date, answer_date, frequency
		from qna, member
		where qna.member_idx = member.member_idx and qna_idx = #{qna_idx}
	</select>
</mapper>